# JPA
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Internationalization
spring.messages.basename=messages/messages

# Application
common.pagination_size=5
product.list.page_request.page_size=10